import React from 'react';

export const LineChart = (props: any) => React.createElement('LineChart');
export const ProgressChart = (props: any) => React.createElement('ProgressChart');
export const PieChart = (props: any) => React.createElement('PieChart');
export const BarChart = (props: any) => React.createElement('BarChart');
export default { LineChart, ProgressChart };
